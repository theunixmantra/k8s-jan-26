apiVersion: apps/v1
kind: Deployment
metadata:
  name: videos-app
spec:
  replicas: 1
  selector:
    matchLabels:
     app: videos
  template:
    metadata:
      labels:
        app: videos
    spec:
      containers:
        - name: nginx
          image: nginx:latest
